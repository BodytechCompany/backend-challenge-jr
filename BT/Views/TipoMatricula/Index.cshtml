@model IEnumerable<BusinessLibrary.Entity.TipoMatricula>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-responsive table-hover ">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoMatricula_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoMatricula_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoMatricula_id })
        </td>
    </tr>
}

</table>
